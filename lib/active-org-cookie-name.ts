/** Which of your companies you're currently working in — the cookie name, shared by the request-
 *  scoped helpers (lib/active-org.ts) and the middleware, which cannot import next/headers. */
export const ACTIVE_ORG_COOKIE = "so_active_org";
